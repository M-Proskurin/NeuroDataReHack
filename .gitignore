# pixi environments
.pixi/*
!.pixi/config.toml

# raw + intermediate data, namespaced by dandiset under data/{raw,processed}/<id>/
# (see CLAUDE.md — never commit NWB / rate matrices / embeddings)
data/raw/
data/processed/

# python cruft
__pycache__/
*.pyc
.ipynb_checkpoints/
/presentation
/reports/figures
